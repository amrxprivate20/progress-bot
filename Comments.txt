There are few notes and fixes that need to be done, I will address them all, and I want you to tell me how to implement the fixes.
General note: always rely on clean tasks names for matching, NEVER rely only tasks ID!
due to usage limits make the fixes one by one and make sure it is working using tail!.
you need to type-check then deploy  then confirm with tail that everything is done correctly!
while going through the code confirm the done notes that they are done properly and if so but a confirmation mark beside them!
Always update this file, to know what is done and what is left!
Always update the start and help commands with the latest commands list!

TODO:
    - Test post-analysis Q&A flow
    - Test report saving with all Q&A combined

DONE (23 JAN 2026 - Session 6 - POST-ANALYSIS Q&A):
    ✅ Added post-analysis questions flow:
       • After AI analysis, if AI has follow-up questions, they're asked to user
       • Report is NOT saved until questions are answered or skipped
       • Pending report state saved in conversation_state table
       • Post-Q&A session similar to pre-analysis Q&A
    ✅ Combined Q&A in saved report:
       • Pre-analysis answers saved with question text
       • Post-analysis answers prefixed with "[متابعة]"
       • All Q&A saved in user_comments field as JSON
    ✅ Updated /skip_questions:
       • Now handles both pre-analysis and post-analysis Q&A
       • Skipping post-analysis Q&A immediately saves report

DONE (23 JAN 2026 - Session 6 - MEMORY DELAY & DEDUP):
    ✅ Added delays between memory operations:
       • 2-second delay before memory updates start
       • 500ms delay between each category update
       • Memory categories updated one by one (not all at once)
       • Skipped memory optimization to reduce subrequests
    ✅ Added task completion deduplication (20-minute window):
       • Tracks recently completed tasks in conversation_state table
       • If same task ID received within 20 minutes, it's ignored
       • Prevents duplicate processing from Todoist webhook retries
       • Records auto-expire after 25 minutes

DONE (23 JAN 2026 - Session 6 - REPORT SAVE & SUMMARY FIXES):
    ✅ Fixed "Too many subrequests" error:
       • Moved report save BEFORE memory updates (critical operation first)
       • Report now saves even if memory update hits Cloudflare limits
       • Reduced Telegram messages to minimize subrequests
    ✅ Added AI-generated day summary:
       • New [DAY_SUMMARY] section in AI prompt
       • AI generates 2-3 sentence summary of the day
       • Saved in obsidian_file_id column (repurposed)
       • Used in past week summary for better context
    ✅ Improved past week summary in AI prompt:
       • Now uses AI-generated summaries when available
       • Falls back to first sentence of AI commentary
       • More condensed goals display

DONE (23 JAN 2026 - Session 6 - REPORT & MEMORY FIXES):
    ✅ Fixed /report command to fetch saved reports:
       • Now checks daily_reports table first for existing saved report
       • If found: displays full report with AI commentary, goals analysis, reward
       • If not found: generates preview and prompts for /confirm
    ✅ Fixed report saving to include full formatted report:
       • report_markdown now saves the full task breakdown (not just AI commentary)
       • ai_commentary saved separately
       • user_comments now saves Q&A responses with the report
    ✅ Improved past week summary in AI prompt:
       • Now includes challenge evaluation results
       • Includes AI commentary excerpt (first 150 chars)
       • Includes goals analysis highlights (completed/neglected counts)
    ✅ Autocomplete respects priority threshold:
       • Low priority subtasks (below threshold) are now ignored
       • Only counts subtasks at or above priority threshold as "pending"

DONE (23 JAN 2026 - Session 6 - AUTOCOMPLETE FIX):
    ✅ Refactored completeParentInTodoistIfAllDone to use Todoist API:
       • Now queries Todoist API directly: GET /tasks?parent_id={parentId}
       • Checks due date for recurring subtasks (future = done for today)
       • Also checks daily_failures JSON for failed subtasks before autocompleting
    ✅ Verified with tail logs - autocomplete working correctly

DONE (22 JAN 2026 - Session 5 - MAJOR FIX):
    ✅ Fixed root cause of autocomplete and notification grouping:
       • ROOT CAUSE: `(origin: parent_name)` marker was added to `description` field but search functions looked in `content` field
       • FIX: Now adds `(origin: parent_name)` marker to task CONTENT when saving subtasks
       • Added Todoist API fallback to fetch parent name if not in DB
    ✅ Improved subtask counting in completeParentInTodoistIfAllDone:
       • Added fallback to match by origin_task ID when origin marker not found
    ✅ Improved sendTaskNotification parent finding:
       • Added Method 3: Find parent by origin_task ID in database
       • Added fallback ID matching for completed subtasks collection
    ✅ Fixed /todayplan infinite loop:
       • Uses update_id idempotency to prevent duplicate processing
       • Uses waitUntil for background AI processing (no webhook timeout)

DONE (22 JAN 2026 - Session 5):
    ✅ Fixed subtask notification grouping:
       • When subtask is completed, now groups under parent even if parent isn't completed yet
       • Creates virtual parent task for display with ⏳ symbol and progress (e.g., "2/3")
       • Shows all completed subtasks under parent in notification
    ✅ Fixed autocomplete of parent task logic:
       • BUG FOUND: Webhook was passing parent_id instead of subtask_id to completeParentInTodoistIfAllDone
       • Fixed both call sites in todoist.ts (line 476 and 710) to pass event.event_data.id (subtask ID)
    ✅ Fixed AI timeout issues:
       • Added 25-second timeout with AbortController to AI client fetch call
       • Reduced retry attempts from 3 to 2 to avoid long waits
       • Prevents Grammy webhook timeout (which was 10 seconds)
    ✅ Fixed [0m] in reports and notifications:
       • Updated getOriginalMetadataString() in task-parser.ts to filter out zero duration patterns
       • Now filters: [0m], [0 دقيقة], [0د], [0h], [0 ساعة], etc.
    ✅ Fixed /todayplan and /tomorrowplan infinite loop:
       • Removed duplicate request detection that was causing issues
       • Simplified to direct execution
    ✅ Fixed /status command error:
       • Fixed variable shadowing bug (op used as loop variable)
       • Added try/catch blocks for resilience
       • Changed to plain text (no Markdown parsing issues)
    ✅ Fixed /cancel command bug:
       • Fixed variable shadowing bug 

DONE (22 JAN 2026 by Claude Code - Session 4 - FIXES):
    ✅ /todayplan and /tomorrowplan lock fix:
       • Removed unnecessary locks from read-only plan commands
       • These commands only read data, they don't need exclusive access
    ✅ /cancel command bug fix:
       • Fixed variable shadowing bug (`op` was used for both DB operator and loop variable)
       • Now properly shows lock info when canceling (e.g. "قفل (/todayplan)")
    ✅ Reduced command lock timeout from 5 minutes to 2 minutes
       • Prevents long waits if a command times out without releasing lock

DONE (22 JAN 2026 by Claude Code - Session 4 - CODE CLEANUP):
    ✅ Removed unused handler files:
       • /src/handlers/reports.ts - Legacy code, functionality moved to report-generator.ts
       • /src/handlers/process-report.ts - Legacy code, functionality moved to Durable Object
    ✅ Removed unused exports:
       • createTodoistClient() from todoist-client.ts - Never used (TodoistClient class used directly)
    ✅ Verified index.ts - No orphaned routes
    ✅ Verified both locking mechanisms are needed:
       • command-lock.ts - In-memory locking for bot commands
       • locks.ts - Database-based locking for Todoist webhook operations
    ✅ Type-check passed, deployed successfully

DONE (22 JAN 2026 by Claude Code - Session 3):
    ✅ AI-POWERED DAY PLANNING - /todayplan and /tomorrowplan now use AI to generate smart plans with:
       • Intelligent task scheduling based on priorities and times
       • Wake up/sleep recommendations
       • Passive tasks (commitments) in separate section
       • Low priority tasks as "if time permits"
       • Personalized tips based on memory/patterns
       • Considers daily challenge, weekly goals, and circumstances
    ✅ Debug logger chunking fixed - splitMessage now handles very long lines, sends as plain text (no Markdown issues), shows chunk indicators [1/N]
    ✅ Full AI response logging - Now logs: commentary, questions, memory updates, goals analysis separately to Telegram

DONE (22 JAN 2026 by Claude Code - Session 2):
    ✅ Wrap All Commands with global Locking - Applied withCommandLock to /confirm, /log_failure, /generate_goals, /sync, /completetask. /createtasks has its own locking.
    ✅ Verified done notes - All checked: extractCleanTaskName used 61 times across 4 files, replaceDuplicateIfExists exists, /clearmemory confirmation works, streak tables use clean names

DONE (22 JAN 2026 by Claude Code):
    ✅ During debugging mode, full unified prompt sent to telegram - FIXED: report-processor.ts now logs complete prompt with all sections
    ✅ manuall failure logging should append the failed tasks to the daily failed task JSON - FIXED: processTaskFailure now appends to daily_failures JSON instead of tasks table
    ✅ in both report and notifications zero duration are still written - FIXED: formatArabicTime returns empty for zero, duration format changed from "30م" to "30 دقيقة"
    ✅ the task completion notification is showing the whole group - VERIFIED: sendTaskNotification already shows all completed and failed subtasks
    ✅ Debugging & AI configuration commands - FIXED: Added /debug to toggle debug mode, /setmodel to change AI model
    ✅ make sure that the task duplication prevention logic is working - VERIFIED: replaceDuplicateIfExists function exists and works
    ✅ a /cancel command exists - VERIFIED: /cancel and /canceltask commands are implemented
    ✅ auto /status should only works when debugging mode is on - FIXED: sendAutoStatus now checks debugger_mode setting
    ✅ detect all the commands and rewrite start/help messages - FIXED: Updated both /start and /help with all commands including /starttask, /completetask, /debug, /setmodel, etc.
    ✅ in the report, the partially completed are not calculated - VERIFIED: calculateStatistics already includes partial_tasks count

DONE:
   - I noticed in supabase streak and tasks tables, tasks names are not clean which leads to duplicates with diffrent paremeters and thus problems in streak tracking because it counts as different tasks
   - the daily challenge widget has (current daily challenge + daily challenges list of current week) but it is missing the summary of this week's daily challenges (it is placed in weekly goals instead which is wrong)
   - The weekly goals widget is missing the last weekly goals analysis and has data that need to be removed (the weekly daily challenges list and summary)
   - not all memory categories are updated, only the first one and other catergories are always empty, there must be a problem!
   - when I complete many tasks almost at the same time in the same time, the autocomplete parent logic and other logic can be triggered multiple times, there must be a lock or something so that logics runs in sequence not at the same time.
   - the analysis data not correct, the ai comment talk about 100% success while in fact the report had 55% success, it is like its talking about another report.
   - the notification message of analysis start .. should include the ai model name for reference.
   - The report preview has an issue which arise when a sub task is completed and its main tasks is still not (specially if the main task is under the priority level), it appears as if it is a main task (separately)
   - the daily challenges result aren't updated and summary is wrong, all challenged where the day passed without being completed must be considered a fail!
   - The parent autocomplete works even when there are still uncompleted sub tasks!
   - adding debug logging to the Durable Object or other areas you think it is useful to see during debugging mode 
   - Using the debugger mode I noticed that the prompts sent to me are trunced! I need them to be complete so I can have the full image of what is going on! same for the  memory updates sent to me, it is also trunced!
   - The memory logic needs revising, because I noticed during multiple logs across many days, the first memory category is the only one that has updates while other categories are still empty for some reason! putting into consideration that the output sometimes may have many categories for memory, or multiple outputs for the same category.
   - Using the debugger mode I noticed that the report sent in the unified prompt is completely diffrent from the report I see in preview using /today or /progress, it is like it is built differently from scratch which is wrong, the system need to send the exact report I see in preview without making another one!    
   - /starttask should find the a simillar task from the task that are STILL AVAILABLE TODAY, not all tasks related to my project.
   - autocomplete parent task should'nt take place if the parent task is already completed today! to prevent duplications.
   - /clearmemory does nothing after asking for confirmation!
   - debugger mode don't send the unified prompt sent to the model it only send the responce SOMETIMES!
   - The response of ai fairly write memory outputs to other memory categories except for the first one, maybe the prompt need fixing!
   - The tasks in the group aren't grouped! all subtasks (failed or completed) should be grouped with their parent tasks (failed or completed)
   - Even if debugger mode is on. still I don't get the unified prompt that is sent to the model .. I want to see it completely!
   - If a command is in process and another command is sent it should be rejected till the first process ends
   - the /cancel prompt should be used to cancel any pending command not just the current conversation
   - /starttask behavior: Without parameters list all available tasks plus “add new task”; with a task name, if exactly one similar task exists do not start it immediately but show a confirmation list with that task as the first item plus “add new task”; ignore and do not store duration if it equals zero.
   - /completetask & /addquantity & /addduration flow: /completetask must immediately complete the current active task with no parameters,/addduration for manually adding duration (with explanation) before completion then resend completion message again, /addquantity for manually add quantity to the current task before completion when needed then resend completion message again, and after completion send a permanent message encouraging the user to start again using /starttask, and revise the autocomplete of parent task logic because it is not working.
   - due to mutliple modifications in the code, now the autocomplete logic for parent task if there no remaing subtask to be completed isnot working. Also the report task grouping is not good enough, as some completed subtasks are not shown and other are shown seperately, other completed subtasks are grouped with a simillar parent leaving their parent.   
    - /log_failure behavior: Acts like /completetask but logs a failure; if called without parameters list all available tasks plus “add new task”; when a task is selected or written register it as a failure, and if the task is recurring postpone it in Todoist to the next due date, if not recurring then delete it from Todoist.
    - If a task with the same name is completed more than once in the same day, then it should be replaced not duplicated!
    - in report view sub-tasks standalone not being grouped in its parent!
    - /log_failure when choosing a task, instead of postponing it after registering it as a daily failure, It completes it in todoist, make is marked as success in both database and todoist!
    - the commands for adding duration and quantity must always be presented in every message with /comletetask
    - /status command fix: Make /status functional so it shows the current task status and whether there is any pending or incomplete command flow awaiting user input, and it should be sent automatically at the end of any process!
